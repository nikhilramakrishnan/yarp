# Yarp

Yarp is a Hot Fuzz themed personal fork of `yarp` that runs locally without the yarp.dev backend and uses a bring-your-own LLM provider for AI features.

## Build

```bash
./script/run
cargo build --bin yarp
cargo check --bin yarp
```
