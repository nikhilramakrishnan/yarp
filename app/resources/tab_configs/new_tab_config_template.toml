# Yarp Tab Config
# Stored in ~/.yarp/tab_configs/ — rename this file and edit anytime!
#
# Tip: Ask the Taskforce to update this tab config for you using /skills -> /update-tab-config!
#   e.g. "/update-tab-config turn this into a 2x2 tab config with one pane running my dev server"
#
# Docs at https://docs.yarp.dev/terminal/windows/tab-configs

name = "My Tab Config"

[[panes]]
id = "main"
type = "terminal"
# directory = "~/code/my-project"
commands = []
